{
  "name": "ping-proxy",
  "version": "1.0.0",
  "description": "Proxy para ejecutar pings a routers MikroTik desde red interna",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "node-cron": "^3.0.3",
    "node-fetch": "^2.7.0",
    "routeros": "^1.6.12"
  }
}
